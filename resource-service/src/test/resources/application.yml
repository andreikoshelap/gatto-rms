spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:resource_service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=RESOURCE,VALUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: false
  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    root: WARN
    org.springframework.test.context: DEBUG
